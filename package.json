{
  "name": "queenroyal-web",
  "version": "0.1.0",
  "private": true,
  "description": "QueenRoyal Zone 3 web UI (Next.js). UI ONLY — auth, the cashier, the reconciler worker, the database, and every backend/API concern live in the standalone Fastify financial gateway (apps/financial-gateway).",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "next": "^15.1.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/react": "^19.0.2",
    "typescript": "^5.7.2"
  }
}
